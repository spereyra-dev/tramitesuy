[package]
name = "search"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Pure deterministic life-event search engine (no DB, no HTTP, no filesystem in src)"

[dependencies]
serde.workspace = true
serde_yaml.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
# per_event.rs (task 29) loads the real `data/` seed through the taxonomy
# loader + validator; the boundary guard still keeps `src` free of it.
taxonomy = { path = "../taxonomy" }
