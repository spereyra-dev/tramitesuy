//! Public value types shared by the engine facade and its consumers.
//! Filled in by units A1–A3 (normalizer, tokenizer, matcher, ranker, …).

/// Placeholder until unit A1 lands the real types.
pub struct Placeholder;
