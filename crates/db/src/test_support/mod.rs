//! Test-support instruments behind the `test-support` feature (task 2):
//! usable by this crate's and dependents' integration tests only — never
//! compiled into production builds.

pub mod sql_counter;
