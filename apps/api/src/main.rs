//! TrámitesUY API binary (axum). Router, handlers, DTOs, and redaction land in
//! slice (c); this stub keeps the workspace compiling from day one.

fn main() {
    println!("api scaffold: router arrives in slice (c)");
}
