export default function Loading() {
  return (
    <section className="status-page" role="status" aria-live="polite">
      <h1>Buscando trámites…</h1>
      <p>Esperá un momento mientras cargamos la página.</p>
    </section>
  );
}
