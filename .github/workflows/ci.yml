name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  lint:
    name: fmt + clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94.1
        with:
          components: rustfmt, clippy
      - name: cargo fmt --check
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: cargo test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: tramitesuy
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94.1
      - name: cargo test --workspace
        run: cargo test --workspace
        # The golden gate runs as its own required job below (task 38,
        # D-7): `cargo test -p search --test golden`.

  # taxonomy-validate:
  #   TODO(task 26/90): wire the DB-free `taxonomy-validate <data-dir> <snapshot-file>`
  #   CLI as a required check once crates/taxonomy ships its CLI.

  golden-gate:
    name: golden dataset gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94.1
      - name: cargo test -p search --test golden
        run: cargo test -p search --test golden
